[pytest]
testpaths = tests
addopts = -m "not llm and not milvus"
markers =
    llm: integration tests requiring a real LLM API key (skipped by default)
    milvus: integration tests requiring a running Milvus server (skipped by default)
