# ── 招标文件解析（M1）──
# 注意：pymupdf 钉 1.28.0 —— 1.28.2 的 Windows wheel 加载 _mupdf.pyd 失败（法律助手项目踩坑结论）
pymupdf==1.28.0
python-docx==1.2.0
openpyxl>=3.1
pillow>=10.0          # 样例扫描件渲染（scripts/make_sample_tender.py）

# ── LLM 客户端（OpenAI 兼容协议，DeepSeek）──
openai>=1.40

# ── HTTP API（端口 8001）──
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
python-multipart>=0.0.9   # 多文件上传

# ── 测试 ──
pytest>=8.0
httpx>=0.27

# ── M1 可选：扫描件 OCR（体积大按需安装；本机已装 paddleocr 3.7.0 + paddlepaddle 3.2.2）──
# 注意 3.x API 与 2.x 完全不同（predict() 替代 ocr.ocr()）；CPU 需设
# PADDLE_PDX_ENABLE_MKLDNN_BYDEFAULT=0（oneDNN NotImplementedError 规避）
# paddlepaddle>=3.0
# paddleocr>=3.0

# ── M2：企业知识库（BGE 嵌入 + Milvus，复用法律助手方案）──
# 本机实测：pymilvus 3.0.0（MilvusClient 新 API）× Milvus standalone 2.3.3 全链路兼容
# （scripts/probe_milvus.py spike 固化）；若换环境不兼容可 pin pymilvus==2.4.x
sentence-transformers
pymilvus>=2.4.0
numpy>=1.26          # SqliteVectorStore 暴力余弦降级路径

# ── M4 引入（Word 导出）──
# docxtpl>=0.16
